from setuptools import setup, find_packages

setup= (name = 'Facebooke_Marketplace-Ranking',
        version = 1.0,
        packages = find_packages(),
        install_requires = [
            alive-progess == 2.4.1,
            pillow == 9.2.0
        ]
        }